Classroom Info
<%= render partial: 'classroom', locals: { classroom: @classroom } %>
<% @classroom.students.each do | stuudents | %>
<%= render partial: 'students/student', locals: { student: stuudents } %>

<% end %>